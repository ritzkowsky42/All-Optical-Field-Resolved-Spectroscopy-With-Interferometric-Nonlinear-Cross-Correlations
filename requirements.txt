h5py>=3.8
matplotlib>=3.7
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Optional: manuscript figure style used by the notebooks
SciencePlots>=2.1

# Optional: only needed to re-fetch HITRAN line data (REFETCH_HITRAN = True
# in fid_analysis.ipynb). Also requires a free HITRAN_API_KEY.
# hitran-api

# To run the notebooks
jupyterlab
